# Build environment for the GitHub Pages demo (Option B).
# Used by the deploy workflow: replays pre-recorded runs, no backend, no keys.
VITE_USE_REPLAY=true
VITE_USE_STREAM=false
VITE_BASE_URL=/smart-search-project/
VITE_ACCOUNT=demo-account
