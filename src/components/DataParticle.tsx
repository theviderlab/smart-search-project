// Placeholder for an SVG-path-following particle. The current implementation
// uses React Flow's built-in animated edges (`animated: true`) to convey data
// flow. If a richer particle effect is desired, this component can be expanded
// to render a Framer Motion <motion.circle> animated along an edge's SVG path.
export default function DataParticle() {
  return null
}
